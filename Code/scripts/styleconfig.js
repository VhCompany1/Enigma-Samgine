let corpo = window.document.getElementById('corpo')

function fonte() {
    if (window.fonte.gb == "no") {
        corpo.style.color = 'black'
    }
    if (window.fonte.gb == "ad") {
        corpo.style.color = 'red'
    } else {}
}